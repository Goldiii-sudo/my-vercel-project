{
  "name": "reelestate-video-generator",
  "version": "1.0.0",
  "description": "ReelEstate — apartment slideshow video generator with NVIDIA NIM (Qwen) and DB integration",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "export-db": "python3 scripts/export_db.py"
  },
  "engines": {
    "node": ">=18"
  }
}
